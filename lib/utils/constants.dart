import 'package:flutter/material.dart';

class AppColors {
  static const primary    = Color(0xFF6C63FF);
  static const secondary  = Color(0xFF3B82F6);
  static const success    = Color(0xFF4CAF50);
  static const warning    = Color(0xFFFF9800);
  static const error      = Color(0xFFF44336);
  static const background = Color(0xFFF8F9FF);
}

class AppStrings {
  static const appName    = 'StudyTracker';
  static const tagline    = 'Smart Study Productivity';
}

class AppConstants {
  static const dailyTargetMinutes  = 240;   // 4 hours
  static const weeklyTargetHours   = 20;
  static const maxStreakBonus      = 20;
}